Paley(23) is NOT 5-inducible -- machine-verified.

MARGIN NOTE   This run is at MAJORITY (every arc >= 3 of 5).  By the 3-cycle
              bound (RESEARCH_LOG 2026-09-04) a linear order agrees with at most
              2 arcs of a cyclic triangle, so over 5 voters the three supports
              sum to <= 10 and none can exceed 4.  Every arc of Paley(23) lies in
              a 3-cycle, so majority is EQUIVALENT to margin<=3 here and this
              verdict covers both.
date          2026-09-05 16:43:52
margin        majority
base          [0, 1, 2, 5, 6, 3]  (3414729 base states)
break         arc (2, 6) + non-arc (1, 6) -> 343896 live cubes
cubes         343896, ALL UNSAT, each INDEPENDENTLY VERIFIED by lrat-trim
solver        cadical --lrat=true --checkproof=0 (self-check disabled)
checker       lrat-trim (Biere) -- different program, different author
NO DEEPENING  cubes are base states of a 6-vertex base; deepen.py unused
solve         228.3 core-h
check         7.7 core-h
wall          21.72 h on 11 workers
ROOT (CNF)    7e6c9c26ac386e675687d28420ac41401c11d6bdbc0d006b9722fff394de49cb
ROOT (proofs) fccd41d05b80d44f7bfa118ed6091f50fa09c4a95e6b4c7afbb945c784651807

TWO ROOTS, AND ONLY THE FIRST IS PORTABLE.
  ROOT (CNF) commits to the sha256 of every cube's CNF, in cube order.  Those
  are generated by this repository's code and ARE reproducible anywhere: a
  third party who regenerates the cube set must obtain this exact value.  It is
  the meaningful cross-check -- it proves they solved the SAME problems.

  ROOT (proofs) additionally commits to the LRAT proof bytes.  These are NOT
  portable.  cadical is deterministic run-to-run on a fixed binary (verified
  here, 3/3 identical), but its heuristics use floating-point scoring, so a
  different version, architecture or optimisation level can search differently
  and emit a DIFFERENT, EQUALLY VALID proof; another solver differs entirely
  (glucose emits 2.36 MB of DRAT where cadical emits 2.79 MB of LRAT).  So this
  value detects corruption or tampering WITHIN this run and reproduces only on
  an identical build.  A mismatch here is NOT evidence of an error.

Proofs verified then discarded; per-cube sha256 of CNF and proof in log/,
per-chunk rolling hashes of both in done/.

Coverage of the cube set: certified separately by cover_check.py at |B|=6
-- the cubes provably cover every lex-canonical base assignment, DRAT proof
independently verified by drat-trim.

The --drop negative control was NOT run, deliberately.  It is not part of the
proof: coverage UNSAT already establishes that nothing bypasses the cubes, and
redundant cubes cost time rather than soundness.  Its only role would be to
exclude a vacuous certificate, and vacuity cannot arise here -- it would require
F_B & SB to be unsatisfiable, i.e. the 6-vertex base not 5-inducible,
contradicting N(5) >= 12.

Remaining HUMAN lemmas (not covered by any certificate):
  (L1) arc-orbit anchoring is WLOG  -- licenses running only the live cubes
  (L2) voters may be lex-ordered WLOG -- licenses the lex chain in coverage
Both proved in RESEARCH_LOG.md.

--------------------------------------------------------------------------
ADDENDUM 2026-09-05: TIMING-FREE ROOT (proofs)

The ROOT (proofs) above hashes a per-cube record that includes WALL-CLOCK
timings, so it differs between two identical runs and cannot be reproduced by
anyone, including this machine -- a stronger limitation than the "not portable
across builds" described above.  Demonstrated: the same two cubes solved twice
under that code gave 0e42a231ad0323d8... and 95315066219b8aa2...

The hashed record now omits timings (they remain in log/k*.log for humans).
Recomputed for THIS run from the preserved per-cube sha256(CNF) and
sha256(LRAT) in log/, with the OLD roots rebuilt first as a parse check --
both matched exactly, so the new value is derived from the same bytes:

  ROOT (proofs, timing-free)
      01e52ba56cacd1c2746902052aaa6d56cb6b4ab49f07cdd34827ce621db4bf1e

That is the value a re-run of this certification on this cadical build must
reproduce.  ROOT (CNF) is unchanged at
      7e6c9c26ac386e675687d28420ac41401c11d6bdbc0d006b9722fff394de49cb
and remains the setup-independent record.

Regenerate with: reroot.py /tmp/p23cert_d6 <cnf_root> <old_proof_root>

--------------------------------------------------------------------------
CORRECTION 2026-09-05: THE COVERAGE CLAIM ABOVE IS NOT YET EARNED FOR THIS RUN

The paragraph beginning "Coverage of the cube set: certified separately by
cover_check.py" is written UNCONDITIONALLY by certify_d6.py, which does not run
the coverage check at all -- that is a separate program.  For the Paley(19)
campaign the claim was true, because coverage was run and verified there.  For
THIS Paley(23) run it has NOT been established: the check was attempted twice on
2026-09-05 and did not complete (the in-memory path needs ~9 GB and was killed
at 8.99 GB).

STATUS OF THIS REFUTATION:
  SEARCH half  DONE.  343,896 cubes, all UNSAT, each LRAT proof independently
               verified by lrat-trim.  Roots as recorded above.
  SPLIT half   NOT DONE.  That the cube set is exhaustive -- i.e. that
               F_B & SB & AND_i not-C_i is UNSAT -- remains to be shown for
               this base.  Until it is, this verdict rests on the cube
               enumeration being exhaustive by construction, which is an
               argument, not a machine-checked certificate.

certify_d6.py has been changed to report coverage status truthfully rather than
assert it.  Re-run with:
  python3 cover_check.py --q 23 --k 5 --margin majority --base 0 1 2 5 6 3 \
      --stream --dimacs <path>.cnf --drat <path>.lrat
(--stream writes the CNF clause-by-clause and solves it with an external
cadical; the in-memory path cannot run this instance.)

--------------------------------------------------------------------------
UPDATE 2026-09-05 18:29: THE SPLIT HALF IS NOW CERTIFIED

The correction above recorded the split half as outstanding.  It has since been
established, so this refutation is complete on both halves.

  cover_check.py --q 23 --k 5 --margin majority --base 0 1 2 5 6 3 --stream
  3,415,435 clauses = 350 F_B + 356 SB + 3,414,729 negated cubes
  (coverage sees ALL base states; the arc/non filter applies to the search half
  only, which is why this is 10x the 343,896 live cubes)
  RESULT UNSAT in 1,182.95 s, 2,097 MiB LRAT proof, VERIFIED by lrat-trim
  peak RSS 5.6 GB, swap unmoved

COMBINED CERTIFICATE (split AND search bound into one value):
  CERT (portable) ff60539e16abf2ecdc1fc822dbb2379abe52fe06f076fe4cf1003bb56ec3bb9e
  CERT (full)     0d017c747352bd5ea701e9daafcc48a4378a2eec812096540bec2bc73d43df74
Hashed blocks in p23cert_d6/p23_cert.{portable,full}.txt -- readable, with the
instance parameters inside, so a value cannot be matched against a different
instance's artifacts.

TOTAL RUNNING TIME ON ONE LAPTOP: 21.72 h (search) + 0.39 h (split) = 22.11 h.

What remains outside the machine-checked part is the two human lemmas:
  (L1) arc-orbit anchoring is WLOG
  (L2) voters may be lex-ordered WLOG
